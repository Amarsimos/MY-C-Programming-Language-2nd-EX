#include <stdio.h>
#define ARR_SIZW 10
int main()
{
        // char a[10] = "Hello";
        // int z = sizeof(a);
        // char *pa = &a;
        // char *pb = &a[5];
        // int q = sizeof(pa);


        // char *cha = "hello world";
        // char p = cha;
        // pa++;
        // int t = pb - pa;
        // printf("%c", p);
        // return 0;
        int *p = NULL;
        char *q = NULL;
        p++;
        q++;

}